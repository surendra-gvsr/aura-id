#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../rollup/dist/bin/rollup" "$@"
else
  exec node  "$basedir/../../../../rollup/dist/bin/rollup" "$@"
fi
# cmd-shim-target=C:/Users/gujju/aura-id-frontend/node_modules/.pnpm/@sentry+nextjs@8.55.2_@open_27925632fc04c0d8e0ef09e6a1563302/node_modules/rollup/dist/bin/rollup
