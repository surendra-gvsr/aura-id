#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../jest-cli/bin/jest.js" "$@"
else
  exec node  "$basedir/../../../jest-cli/bin/jest.js" "$@"
fi
# cmd-shim-target=C:/Users/gujju/aura-id-frontend/node_modules/.pnpm/jest@30.4.2_@types+node@20.19.41/node_modules/jest-cli/bin/jest.js
